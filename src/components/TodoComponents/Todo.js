import React from 'react';

import { data } from ''

// takes in the `todo` data and displays the task to the screen.

const Todo = (data) => {
    return(
        <div>
            task
        </div>
    );
};

export default Todo;